<div class="container">
  <div class="mb-4">
    {{#link-to "posts"}}
      <i class="fa fa-long-arrow-left" aria-hidden="true"></i>
      Return
    {{/link-to}}
  </div>
  <PostForm @onSubmit={{action "createPost"}}>
    <BsButton
      @defaultText="Create"
      @type="success"
      @buttonType="submit"
    />
  </PostForm>
</div>